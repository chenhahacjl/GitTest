# GitTest
git branch local  
git pull origin master  
git checkout local  
git merge master  
\# Do STH  
git add .  
git commit -m "Commit"  
git checkout master  
git merge local  
git push origin master:master  
